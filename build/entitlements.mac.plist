<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- Entitlements applied under Apple's "hardened runtime" (required for notarization).
     These are the standard set an Electron app needs: V8's JIT (allow-jit +
     allow-unsigned-executable-memory), Electron's use of DYLD env vars, and
     disable-library-validation so the app can run its bundled, separately-signed
     FFmpeg engine without Apple's library-validation blocking it. -->
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
